{
  "name": "bropay",
  "version": "1.0.0",
  "description": "Next-Gen AI Banking Platform",
  "main": "index.html",
  "scripts": {
    "dev": "python3 -m http.server 3000",
    "build": "mkdir -p dist && cp -r *.html css js dist/ 2>/dev/null || true && echo 'Static files copied to dist'",
    "vercel-build": "echo 'Vercel build complete - no build needed for static site'",
    "deploy": "vercel --prod"
  },
  "keywords": ["banking", "crypto", "ai", "finance"],
  "author": "bedusec",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/umenyi-bryan/BROpay.git"
  }
}
